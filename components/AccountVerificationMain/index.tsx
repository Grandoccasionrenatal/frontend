'use client';

import { useMutation } from '@tanstack/react-query';
import { useSearchParams } from 'next/navigation';

const AccountVerificationMain = () => {
  const searchParams = useSearchParams();

  const {} = useMutation({});

  return <></>;
};

export default AccountVerificationMain;
